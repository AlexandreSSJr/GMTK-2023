using UnityEngine;

public class Tile : MonoBehaviour
{
    [SerializeField] public Env.Paths path = Env.Paths.Empty;
    [SerializeField] public Env.Slots slot = Env.Slots.Empty;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
